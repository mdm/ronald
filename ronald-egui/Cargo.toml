[package]
name = "ronald-egui"
version = "0.1.0"
edition = "2024"

[dependencies]
ronald-core = { path = "../ronald-core" }
eframe = { version = "0.31.1", default-features = false, features = [
  "accesskit",
  "default_fonts",
  "persistence",
  "wgpu",
  "x11",
] }
serde = "1.0.219"
